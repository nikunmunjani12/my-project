import 'package:flutter/material.dart';

class Allprectice extends StatelessWidget {
  Allprectice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [ElevatedButton(onPressed: () {}, child: Text("hello"))],
      ),
    );
  }
}
